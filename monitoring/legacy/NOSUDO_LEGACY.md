# Legacy No-Sudo Monitoring

`monitoring/nosudo` is now legacy and not the default path.

Current default deployment is root-level Docker Compose:

```bash
cd /home/jiangzhiming/workspace/rag-stack
bash scripts/stack_up.sh
```

Historical no-sudo assets remain in:
- `/home/jiangzhiming/workspace/llm/rag/monitoring/nosudo`
